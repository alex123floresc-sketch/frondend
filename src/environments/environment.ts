export const environment = {
  production: false,
  // URL base de tu backend Spring Boot en desarrollo.
  // El backend escucha en el puerto configurado en application.yml (PORT, por defecto 9091).
  apiUrl: 'http://localhost:9091/api'
};
